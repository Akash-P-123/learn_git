# TO Initiate the git
git  init

# To add remote repo
git remote add origin url
git fetch origin main

# To Pull all the remote files to the local
git push origin main

# How to clone a repo
git clone "URL"
git clone https://<username>:<pat>@github.com/<username>/<repo>.git

# How to check status , commit , add and push your repo in your git hub
git status
git add
git commit -m "Feat: comment " 
git commit --amend # Used for to edit the last commit in a test editor and also we can able to use the same commit message again 
git push
git push -f #force the push operation

#How to rename file_name
git mv old_file_name new_file_name 

#How to check your No. Of Branches in your repo 
git branch -a
git chekout branch_name

# How to  delete a branch 
git branch -d branch_name

# To delete in github interface/isolated branch we use 
git push origin --delete branch_name

# To delete a file in the branch
git rm file_name
git commit -m "Feat: comments"
git push origin main

#The Git reset Command
git reset --medium hash value 
git reset --soft hash value
git reset --hard hash value

#To pick the spefic feature from another branch 
git cherry-pick <Hash_Value>

#To squash commit (or) Reorder the commit in Git we use
git rebase -i <previous_hash_value>

# The git Stash Command
git stash -u # Used for unstaged file to save temporary in local for to checkout the current branch
git stash #Used for Staged file to save
git stash list # Shows the lish of file which are stashed
git stash pop # Used for to reterive the stased file ( works on LIFO)
git stash clear
git stash show
git stash drop
git stash apply

#The Git diff Command is used to view the difference between the old file and updated file
git diff
git diff --staged
git diff --w
git diff <Hash_Value_1> < Hash_Value_2>
git diff --no-index
git diff --word-diff
git diff --stat


#The Git log Command
git log --oneline
git log 
git log --oneline --decorate
git log --stat
git log --shortlog
git log --graph --oneline --decorate
git log -p
git log --pretty=formate:"%cn %h %cd"
git log --after="2025-1-1"
git log --grep="hi"

#The git ignore command
file_name.gitignore

# TO create a new branch and merge the branch
git checkout -b branch_name
git merge branch_name #  to merge we need to be in main branch

